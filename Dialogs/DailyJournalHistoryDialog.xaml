<Window x:Class="WorkAudit.Dialogs.DailyJournalHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Daily Journal History" Height="500" MinHeight="400" Width="600" MinWidth="500"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource ContentBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="View Past Journal Entries" FontSize="16" FontWeight="SemiBold"
                   Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Select Date:" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource PrimaryForeground}"/>
            <DatePicker x:Name="JournalDatePicker" Grid.Column="1" SelectedDateChanged="JournalDatePicker_SelectedDateChanged"/>
            <Button x:Name="TodayBtn" Grid.Column="2" Content="Today" Padding="10,6" Margin="8,0,0,0"
                    Background="#17A2B8" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="TodayBtn_Click"/>
        </Grid>

        <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                Background="{DynamicResource PanelBackground}" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="EntryDateText" Text="No entry for selected date" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="EntryContentText" Text="" TextWrapping="Wrap"
                               Foreground="{DynamicResource PrimaryForeground}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CloseBtn" Content="Close" Width="80" Height="32" Click="CloseBtn_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}" Foreground="{DynamicResource PrimaryForeground}"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
