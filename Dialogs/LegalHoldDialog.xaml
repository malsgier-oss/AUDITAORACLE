<Window x:Class="WorkAudit.Dialogs.LegalHoldDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply Legal Hold"
        Width="400" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource ContentBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Case number (required):" Margin="0,0,0,4" Foreground="{DynamicResource PrimaryForeground}"/>
        <TextBox x:Name="CaseNumberBox" Grid.Row="1" Margin="0,0,0,12" Padding="8,6"/>
        <TextBlock Grid.Row="2" Text="Reason (required):" Margin="0,0,0,4" Foreground="{DynamicResource PrimaryForeground}"/>
        <TextBox x:Name="ReasonBox" Grid.Row="3" Margin="0,0,0,16" Padding="8,6" 
                 TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60" VerticalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkBtn" Content="OK" Width="80" Height="28" Margin="0,0,8,0"
                    Click="OkBtn_Click" IsDefault="True"/>
            <Button x:Name="CancelBtn" Content="Cancel" Width="80" Height="28"
                    Click="CancelBtn_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
