<Window x:Class="WorkAudit.Dialogs.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AUDITA – Sign In" Height="380" Width="400" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        Background="{DynamicResource ContentBackground}">
        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock x:Name="ProductTitleText" Text="AUDITA v1.0" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}" TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBlock x:Name="TaglineText" Text="Audit Workspace for Documents, Notes &amp; Reporting" FontSize="11" Foreground="{DynamicResource SecondaryForeground}" Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>

            <!-- Username -->
            <TextBlock x:Name="UsernameLabel" Grid.Row="1" Text="Username" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Grid.Row="2" Padding="10,8" Height="36" Margin="0,0,0,16"
                     Background="{DynamicResource ContentBackground}" Foreground="{DynamicResource DimForeground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

            <!-- Password -->
            <TextBlock x:Name="PasswordLabel" Grid.Row="3" Text="Password" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,4"/>
            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <PasswordBox x:Name="PasswordBox" Padding="10,8" Height="36" VerticalAlignment="Top"
                                     Background="{DynamicResource ContentBackground}" Foreground="{DynamicResource DimForeground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                     PasswordChanged="PasswordBox_PasswordChanged"/>
                        <TextBox x:Name="PasswordPlain" Padding="10,8" Height="36" VerticalAlignment="Top"
                                 Background="{DynamicResource ContentBackground}" Foreground="{DynamicResource DimForeground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                 Visibility="Collapsed" TextChanged="PasswordPlain_TextChanged"/>
                    </Grid>
                    <Button x:Name="TogglePasswordVisibilityBtn" Grid.Column="1" Width="36" Height="36" Margin="4,0,0,0"
                            VerticalAlignment="Top" Padding="0" Click="TogglePasswordVisibility_Click"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            KeyboardNavigation.IsTabStop="False">
                        <!-- Inline font + glyph: avoid StaticResource to IconFont/IconViewPassword (can fail before App merged dictionaries resolve at startup). -->
                        <TextBlock x:Name="PasswordVisibilityIcon" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                   Foreground="{DynamicResource PrimaryForeground}" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Text="&#xE890;"/>
                    </Button>
                </Grid>
                <TextBlock x:Name="ErrorText" Grid.Row="1" Foreground="#F14C4C" FontSize="12"
                           TextWrapping="Wrap" Visibility="Collapsed"/>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="LoginButton" Content="Sign In" Width="100" Height="32" Margin="0,0,8,0"
                        Background="#0E639C" Foreground="White" BorderThickness="0" Cursor="Hand"
                        Click="Login_Click" IsDefault="True"/>
                <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="32" Background="{DynamicResource ButtonSecondaryBackground}" Foreground="{DynamicResource DimForeground}"
                        BorderThickness="0" Cursor="Hand" IsCancel="True" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
