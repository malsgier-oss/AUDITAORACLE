<Window x:Class="WorkAudit.Dialogs.SaveReportConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Report Configuration" Height="180" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Enter a name for this report configuration:" Margin="0,0,0,8"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Padding="8,6" FontSize="14"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="OkBtn" Content="Save" Width="80" Padding="12,6" Margin="0,0,8,0" IsDefault="True" Click="OkBtn_Click"/>
            <Button x:Name="CancelBtn" Content="Cancel" Width="80" Padding="12,6" IsCancel="True" Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
