<UserControl x:Class="WorkAudit.Views.ExecutiveDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Background="{DynamicResource PanelBackground}" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="Executive Dashboard" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}"/>
                <TextBlock x:Name="PeriodText" Text="" FontSize="14" Foreground="{DynamicResource SecondaryForeground}" VerticalAlignment="Center" Margin="16,0,0,0"/>
                <Button x:Name="ExportPdfBtn" Content="Export PDF" Padding="16,8" Margin="24,0,0,0" 
                        Background="#0078D4" Foreground="White" BorderThickness="0" Click="ExportPdfBtn_Click"/>
            </StackPanel>

            <WrapPanel x:Name="KpiCardsPanel" Grid.Row="1" Margin="0,0,0,16">
                <!-- KPI cards populated in code-behind -->
            </WrapPanel>

            <Grid Grid.Row="2" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource ContentBackground}" Padding="16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Volume by Branch (double-click to drill down)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,8"/>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <oxy:PlotView x:Name="BranchChart" Grid.Column="0" Height="200"/>
                            <ListBox x:Name="BranchList" Grid.Column="2" Height="200" BorderThickness="0" Background="Transparent"
                                     MouseDoubleClick="BranchList_MouseDoubleClick" Cursor="Hand">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Display}" FontSize="12" Margin="0,2"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Grid>
                </Border>
                <Border Grid.Column="2" Background="{DynamicResource ContentBackground}" Padding="16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="Risk &amp; Anomalies" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,12"/>
                        <ListBox x:Name="RiskList" Height="220" BorderThickness="0" Background="Transparent" Cursor="Hand"
                                 MouseDoubleClick="RiskList_MouseDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Display}" TextWrapping="Wrap" FontSize="12" Margin="0,2"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Background="{DynamicResource ContentBackground}" Padding="16" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="6-Month Volume Trend" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,12"/>
                    <oxy:PlotView x:Name="TrendChart" Height="180"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="5" Background="{DynamicResource ContentBackground}" Padding="16" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Top Issues (by branch)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForeground}" Margin="0,0,0,12"/>
                    <ListBox x:Name="IssuesList" BorderThickness="0" Background="Transparent" Cursor="Hand"
                             MouseDoubleClick="IssuesList_MouseDoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" Margin="0,2" Background="#4A1C1C" BorderBrush="#6B2D2D" BorderThickness="1" CornerRadius="2">
                                    <TextBlock Text="{Binding Display}" TextWrapping="Wrap" FontSize="12" Foreground="#E8B4B4"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
