<Window x:Class="WorkAudit.Views.Admin.EditDocumentTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Document Type" Height="380" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource ContentBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" Padding="6,4"/>

        <TextBlock Grid.Row="1" Text="Section:" VerticalAlignment="Center" Margin="0,12,0,0"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="SectionCombo" Padding="6,4" Margin="0,12,0,0" IsEditable="False"/>

        <TextBlock Grid.Row="2" Text="Keywords:" VerticalAlignment="Top" Margin="0,12,0,0"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,12,0,0">
            <TextBox x:Name="KeywordsBox" Padding="6,4" Height="60"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            <TextBlock Text="Comma-separated keywords for classification"
                       Foreground="{DynamicResource SecondaryForeground}" FontSize="10" Margin="0,2,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Display order:" VerticalAlignment="Center" Margin="0,12,0,0"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="OrderBox" Width="60" HorizontalAlignment="Left" Padding="6,4" Margin="0,12,0,0"/>

        <CheckBox Grid.Row="4" Grid.Column="1" x:Name="ActiveCheck" Content="Active" IsChecked="True" VerticalAlignment="Top" Margin="0,12,0,0"/>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Text="Applied to all branches" Foreground="{DynamicResource SecondaryForeground}" FontSize="11" Margin="0,12,0,0"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="SaveBtn" Content="Save" Padding="16,8" Margin="0,0,8,0" Click="Save_Click"
                    Background="#0E639C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="CancelBtn" Content="Cancel" Padding="16,8" Click="Cancel_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}" Foreground="{DynamicResource PrimaryForeground}" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
