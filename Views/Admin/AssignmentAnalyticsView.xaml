<UserControl x:Class="WorkAudit.Views.Admin.AssignmentAnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf">
    <Grid Background="{DynamicResource PanelBackground}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Assignment Analytics" FontSize="20" FontWeight="SemiBold" Foreground="#212529" Margin="0,0,0,16"/>
        <TextBlock Grid.Row="0" Text="Workload distribution by user" FontSize="12" Foreground="#6C757D" Margin="0,28,0,-32" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Status filter:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="StatusFilterCombo" Width="140" Margin="0,0,16,0" SelectionChanged="Filter_Changed"/>
            <Button x:Name="RefreshBtn" Content="Refresh" Padding="12,6" Click="RefreshBtn_Click"
                    Background="#0E639C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <Border Grid.Row="2" Background="{DynamicResource ContentBackground}" Padding="16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Assignments by User (Pending + In Progress)" FontSize="14" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,12"/>
                <oxy:PlotView x:Name="WorkloadChart" Grid.Row="1" Height="300"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
